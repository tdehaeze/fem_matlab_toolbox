% Function description
% :PROPERTIES:
% :UNNUMBERED: t
% :END:

function [modes_i] = sortModes(z, w, xi, args)
% sortModes -
%
% Syntax: [modes_i] = sortModes(z, w, xi, args)
%
% Inputs:
%    - z, w, xi, args -
%
% Outputs:
%    - modes_i -

end

% Arguments
% :PROPERTIES:
% :UNNUMBERED: t
% :END:

arguments
end
